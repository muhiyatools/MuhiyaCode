
**Live-test findings (post-N, from a real resumed session) — FIXED, full `check.ps1 -Serial` green:**
- **R1 resume fidelity:** reopened sessions rendered tool rows WITHOUT their target (the file/command/pattern/query) because the durable `events` table stored only each tool's output; live rows built `target` from the input args, resume could not. Fix: shared `contract.ToolTarget`/`PatchTargetFile` (lifted from tui, single source of truth), a new `events.target` column (idempotent `ALTER TABLE` migration for pre-existing DBs + column in CREATE for new ones), `Persistence.AddEvent` hook + `db.AddEvent` gain a target param, the engine persists the redacted `ToolTarget(name,args)` at the tool-persist site, `Events`/`TranscriptPage` select it, and the tui resume paths (`transcriptEventToItem` + `loadEvents`) set `toolView.target` from it. `deriveToolTarget` remains the output-based fallback for pre-column sessions. Tests: contract `TestToolTarget`/`TestPatchTargetFile`, state `TestEventTargetRoundTrip`/`TestEventsTargetColumnMigration` (legacy-DB ALTER + idempotency), tui `TestResumeKeepsToolTarget`.
- **R2 run_shell outcome polish:** every shell row showed "exit code: N" (toolOutcome fell through to the first output line) — noisy "exit code: 0" on success, and the exit code was not parsed for failure state. Fix: a `run_shell` case → `shellOutcome`/`parseExitCode` (success shows the output summary or "ok"; failure shows a tight "exit N"). Tui-only, no golden impact. Tests: `TestShellOutcome`, `TestRunShellRowNoNoisyExitZero`.

**R3 (live-test finding #3) — thinking-stream leak — FIXED, full `check.ps1 -Serial` green.** Raw DeepSeek reasoning text streamed into the visible activity area as a faint gutter line (render_header.go's "live reasoning tail", Experience Overhaul A2/D2 design). User: remove entirely. Fix = full pipeline amputation, not a hide: bridge no longer wires `ReasoningToken` (nil switches the stream off at the SSE accumulator source — zero channel traffic/frames), `streamMsg` drops its reasoning field, `appendStream` drops the reasoning branch, `m.reasoning`/`thoughtStart`/`lastThoughtDuration` fields deleted with their clears (keys/modals/update), and the reasoning-tail render block deleted. Spinner + status verb remain the only working indication (Claude-Code-like). contract.Callbacks.ReasoningToken and gateway parsing stay intact (Reasoning still accumulates for replay policy — only the UI stream is gone). Tests: TestBridgeNeverStreamsThinking (pins ReasoningToken unwired), TestThinkingTextNeverRenders (busy+post-task frames clean), TestReasoningTailRuneSafe + TestThinkingIsEphemeralAfterCompletion removed (superseded — the buffer they tested no longer exists), render_language fixture updated.
