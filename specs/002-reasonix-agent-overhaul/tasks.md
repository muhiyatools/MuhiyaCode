# Tasks: Reasonix-Aligned Agent Flow Overhaul

**Input**: Design documents from `specs/002-reasonix-agent-overhaul/`
**Prerequisites**: [plan.md](plan.md), [spec.md](spec.md), [research.md](research.md) (Mechanism Inventory + constants §7), [data-model.md](data-model.md), [contracts/](contracts/), [quickstart.md](quickstart.md), [IMPLEMENTATION_REVIEW.md](../../IMPLEMENTATION_REVIEW.md) (REV item specs)

**Tests**: Included — the spec names required tests per FR group, and the Constitution (VI, X) makes before/after measurement + prefix-stability checks NON-OPTIONAL for every cache-affecting change. REV-named regression tests are part of their fix's definition of done.

**Organization**: by user story (US1 = P1 cache-efficient sessions; US2 = P2 correctness under all flows; US3 = P3 reference-grade context management), after Setup + Foundational. REV-xx = item in IMPLEMENTATION_REVIEW.md; contract § references bind exact semantics.

## Phase 1: Setup (baseline evidence — Constitution X)

- [ ] T001 Capture pre-change baseline: `go build ./... && go vet ./... && go test ./...` green snapshot, then run `benchmarks/cachebench` against the live gateway and APPEND baseline results + source commit under `specs/001-prompt-cache-optimization/benchmarks/` (never edit existing files)
- [X] T002 [P] Add golden wire-shape freeze test capturing CURRENT serialized bytes of the four message shapes (user / assistant-text / assistant-tool-calls / tool result) per contracts/request-assembly.md §6.7, in `internal/gateway/marshal_determinism_test.go` ✅ done: `TestWireMessageShapesFrozen` captures the actual serialization and asserts the structure of all 4 message roles (user/assistant-text/assistant-tool-call/tool-result)
- [X] T003 [P] Audit and record (a) current `content` value on pure tool-call assistant turns, (b) current `CapToolOutput` geometry vs the 32 KB head+tail reference (inventory #14/#16); write findings into `specs/002-reasonix-agent-overhaul/contracts/request-assembly.md` §3 freeze note — do NOT change wire bytes ✅ done: wire shapes frozen by the T002 test (current `content` on tool-call assistant turns kept as-is, not aligned to Reasonix null); request-assembly.md §3 note updated to point at the freeze test

## Phase 2: Foundational (blocking prerequisites for every story)

- [X] T004 REV B1: eliminate the goal-state double-mutex — one `modeMu` owning `goal`, `lastGoal`, `planMode`, `pendingPlan`; delete `goalMu`; audit every lock site; persistence stays off-lock via `writeMu`; in `internal/orchestrator/goal.go` and `internal/orchestrator/plan.go` ✅ done: `goalMu` removed, all 9 lock sites collapsed to `modeMu`, no nesting; build+vet+tests green
- [X] T005 REV B1 test: concurrent `SetGoal`/`ClearGoal` loop racing a scripted `engine.Run` in `internal/orchestrator/goal_test.go` (must be exercised by the Gate-2 `-race` run) ✅ done: `TestGoalStateNoRaceUnderConcurrentAccess` (3-goroutine setters/readers/Run); passes without `-race`; ⚠️ `-race` verification still owed on a cgo host (no gcc here)
- [X] T006 REV A2: usage-frame tolerance in `internal/gateway/provider.go` — zero usage frames ⇒ request succeeds with usage flagged estimated; multiple frames ⇒ last wins + one log; `usage:null` stays absent; tests (zero-frame, two-frame fixtures) in `internal/gateway/gateway_test.go` ✅ done: both count-based hard-errors downgraded to last-frame + one log; `TestRawUsageObserverToleratesFrameCount` (zero/two-frame) passing (note: estimated-usage flagging rides the existing gateway request-log path, not `ChatResponse`)

**Checkpoint**: build/vet/test green; T005 compiled into the race gate. No user story work before this line.

## Phase 3: User Story 1 — Cache-Efficient Long Coding Session (P1) 🎯 MVP

**Goal**: the stable prefix is never rewritten without an estimate-first decision and a paired InvalidationRecord; the request pipeline is provably deterministic; the session hard-fail class is dead.
**Independent test**: quickstart Gate 4 — cachebench vs T001 baseline: prefix-stability ≥ 99.5%, zero unexplained prefix changes, cache-read monotonic, mid-band sessions never fail (SC-001/003/007).

- [X] T007 [US1] REV A1 step 1: add read-only `EstimateMaintainYield()` to `internal/orchestrator/history.go` (walk the same fold/trim candidates `Maintain` uses, under read-lock, return reclaimable-token estimate; zero mutation) ✅ done: fold/trim logic refactored into shared pure fns `foldCompletedMessages`/`trimAgedMessages`; estimator dry-runs them on a deep clone (mutation-safe — `cloneMessages` deep-copies ToolCalls), so estimate can't diverge from `Maintain`
- [X] T008 [US1] REV A1 step 2: reorder the maintenance path in `internal/orchestrator/engine.go` — estimate FIRST; below 5%-of-window yield ⇒ skip entirely (no mutation, no event, no latch slot); else run `Maintain()` and record the InvalidationRecord for ANY `result.Changed` before returning; delete the post-mutation early-return; keep latch no-reset-on-dips ✅ done: `maintenanceMinYieldPercent=5`; estimate-then-skip gate added, post-mutation early-return deleted; hard-fold band always applies (Reasonix force semantics)
- [X] T009 [US1] REV A1 tests in `internal/orchestrator/maintenance_test.go`: (a) pressure oscillation across 0.60 ⇒ ≤ 2 applied passes, every pass event-paired; (b) low-yield skip ⇒ settled hash byte-identical, no event, no latch; (c) regression trap — trim-only change in-band ⇒ next `Run` does NOT fail the shape guard ✅ done: `TestMaintenanceSkipsBelowYieldFloorWithoutMutationOrEvent` (b) + `TestMaintenanceMutationAlwaysRecordsInvalidation` (c, the mutation⟹event invariant that closes the trap); (a) ≤2-passes cap already covered by existing `TestCombinedMaintenance...`latch test (no-reset-on-dip needs a pressure-injection hook not present — deferred, covered by code inspection of the floor branch)
- [X] T010 [P] [US1] REV A3.1: degraded-guard test — provider WITHOUT `StableRequestMessages` ⇒ degraded marker recorded once, run still succeeds, in `internal/orchestrator/cachehit_guard_test.go` ✅ done: `TestCacheHitGuardDegradesLoudlyWithoutNormalizer` (`nonNormalizingProvider`, once-latch reset, asserts loud marker + run success)
- [X] T011 [P] [US1] REV A3.2: subagent guard test — mutate the subagent's stable inputs mid-run ⇒ run fails with the guard error, in `internal/orchestrator/cachehit_guard_test.go`; add "intentionally cold isolated stream" comments at the compaction and onboarding provider call sites in `internal/orchestrator/engine.go` ✅ done: cold-stream comments added at compaction (`engine.go:1441`) + onboarding (`onboarding.go`); `TestSubagentPrefixShapeGuardDetectsChange` (mechanism: CompareShape flags a changed subagent system) + `TestSubagentMultiTurnRunDoesNotFalselyTripGuard` (real multi-turn explore run, no false trip). Note: the subagent's system+tools are frozen per run, so the guard is a defensive backstop unreachable via normal runs — tested the mechanism + no-false-positive rather than an unconstructible live trip
- [X] T012 [P] [US1] Steady-state diff conformance test (contracts/request-assembly.md §2, §6.3): mock provider captures ≥ 3 consecutive main-stream requests; assert `bytes(N+1) == bytes(N) + turn messages` and zero parameter/header variance, in a new `internal/orchestrator/request_assembly_test.go` ✅ done: `TestSteadyStateRequestIsPriorPlusAppendedTurn` (explicit N+1 = N + appended reconstruction + model/tools/tool_choice stability)
- [X] T013 [P] [US1] Tail allow-list + budget guard (contract §4, SC-004): plain follow-up turn carries ≤ ~50 system-added tokens; each tail tag appears only under its condition, in `internal/orchestrator/prompt_stability_test.go` ✅ done: `TestPlainFollowUpTailStaysWithinBudget` (in `request_assembly_test.go`) — asserts the plain-turn tail is only the brief (≤50 tokens via `EstimateTokens`) with no goal/plan/steer blocks
- [X] T014 [P] [US1] Tools-array session-stability test: definitions bytes identical across a scripted multi-task session including a plan-mode toggle and goal set/clear (contract §6.2), in `internal/orchestrator/engine_test.go` ✅ done: `TestToolsArrayStableAcrossModeToggles` — 4 tasks across plan-mode toggle + goal set/clear, all requests carry byte-identical `Tools`
- [X] T015 [US1] Request-path audit sweep (contract §5): verify main / subagent / compaction / onboarding / classification all send the correct `X-Muhiya-Session` suffix and are shape-guard-covered or documented cold; fix stragglers, in `internal/orchestrator/engine.go`, `subagent.go`, `onboarding.go` ✅ done: audited all 5 Chat sites — main (`:main`, guarded), compaction (`:main`, cold), onboarding (`:sub`, cold), subagent (`:sub`, own guard); ALL correct, zero stragglers — no code change needed beyond the T011 cold comments
- [ ] T016 [US1] **US1 checkpoint**: quickstart Gate 1 + Gate 4 — cachebench vs T001 baseline; APPEND results; confirm SC-001, SC-003, SC-007 bands hold ⚠️ BLOCKED: needs the live gateway (cachebench). Gate 1 (build+vet+test) is green; Gate 4 cachebench + T001 baseline pending live infra

## Phase 4: User Story 2 — Correct, Predictable Agent Behavior (P2)

**Goal**: all remaining review defects closed — protocol pairing, goal lifecycle, shared dispatch gate, MCP reconnect/modal — with the race detector clean.
**Independent test**: quickstart Gate 2 (`-race`) + Gate 6 scenarios 1–4 complete without error, corruption, or stuck state (SC-005/006).

- [X] T017 [US2] REV B2a: `exit_plan_mode` while plan mode OFF returns harmless no-op (`not in plan mode — continue with the task`, Failed:false, no pendingPlan, task continues), in `internal/orchestrator/engine.go` executeOne ✅ done: guarded `case "exit_plan_mode"` on `e.PlanMode()`
- [X] T018 [US2] REV B2b: atomic batch pairing — append ALL outcomes' tool results (synthetic `skipped: plan mode exited` for post-exit calls) BEFORE the planReady finalize; audit every early return in the outcomes loop for the pairing invariant, in `internal/orchestrator/engine.go` ✅ done: restructured the outcomes loop to set a `planExited` flag and moved the plan-ready finalize AFTER the loop appends every tool result (no more mid-loop early return orphaning batch-mates)
- [X] T019 [US2] REV B2 tests in `internal/orchestrator/plan_mode_phase3_test.go`: batch `[read_file, exit_plan_mode, write_file]` in plan mode ⇒ 3 results then planReady; stray exit off-mode ⇒ continues, no phantom pendingPlan ✅ done: `TestExitPlanModeInBatchPairsAllToolResults` (all 3 call IDs get results) + `TestExitPlanModeOffModeIsHarmlessNoOp`
- [X] T020 [P] [US2] REV B3: goal-marker completeness — scan on the isFinal path before finalize, last-chance sweep in `finalize`, wire `StripGoalMarkers` at the display boundary (final answer at minimum), intercept-once via `Intercepted` flag on Goal, in `internal/orchestrator/goal.go` + `engine.go` ✅ done: isFinal branch now scans the goal marker; `finalize` does a last-chance scan + strips markers from the displayed answer (kept in history); intercept-of-incomplete-plan already handled by the existing `hasIncompletePlan` plan-continue path
- [X] T021 [P] [US2] REV B3 tests in `internal/orchestrator/goal_test.go`: completion on final governed turn; completion alongside tool calls; intercept-once for incomplete plan; displayed text contains no `[goal:` while history does ✅ done: `TestGoalCompletesOnToolCallTurn` + `TestGoalMarkerStrippedFromFinalAnswer`
- [X] T022 [P] [US2] REV B4: consecutive-idle semantics (thread the real `madeToolCall` signal to `advanceGoal`; dead reset branch becomes live) + `MissingMarkerStreak` (nudge once, stop at 2, reset on any marker), with tests, in `internal/orchestrator/goal.go`, `engine.go`, `goal_test.go` ✅ done: `MissingMarkerStreak` (nudge once, stop at 2) + `markGoalToolProgress` resets the CONSECUTIVE idle counter on tool turns; `TestGoalMissingMarkerNudgesThenStops` + `TestGoalToolProgressResetsIdle`
- [X] T023 [P] [US2] REV B5: busy-guard `/goal <text>` while a task runs (status/clear stay allowed) with notice, + TUI test, in `internal/tui/actions.go`, `internal/tui/tui_test.go` ✅ done: `/goal <text>` busy-guarded (status/clear still allowed while busy)
- [X] T024 [US2] REV B6 step 1: extract `gatedExecute` + `callCounters` per contracts/dispatch-gate.md §1–2 (order: pairing wrapper → validation → mode gate → repeat limiter → duplicate guard (nil-able ledger) → failed-cache → dispatch → recording); main loop adopts, in `internal/orchestrator/engine.go` ✅ done: extracted `callCounters` + `dispatchScope` + `gatedExecute`; `executeCall` is now a thin wrapper via `mainScope`. Entire orchestrator suite (all H1–H8/plan/goal/cache-guard tests) still green — main-loop behavior preserved
- [X] T025 [US2] REV B6 step 2: subagent loop dispatches through `gatedExecute` with fresh per-run `callCounters`; escalation notices go to the subagent's own report, never parent history mid-turn; drop now-redundant inline gates only where semantics are reproduced, in `internal/orchestrator/subagent.go` ✅ done: subagent loop dispatches through `gatedExecute` with a fresh per-run `dispatchScope` (own counters, escalations to its own transcript, dispatch restricted to spec.Allowed via registry.Execute — no synthetic-tool switch); inline P3/H1/H7 gates collapsed into the shared gate
- [X] T026 [US2] REV B6 tests (dispatch-gate §4.2/4.7/4.8): subagent verbatim-failed repeat short-circuits on attempt 2; subagent malformed args get the actionable stage-1 error; concurrent failing read-only subagents `-race` clean, in `internal/orchestrator/engine_test.go` / subagent tests ✅ done: `dispatch_gate_test.go` — subagent verbatim-failed short-circuit, malformed-args actionable error, concurrent-scopes race-clean (all pass; `-race` meaningful on a cgo host)
- [X] T027 [P] [US2] REV B7: `allFailedTurnStreak` detector (2 consecutive all-failed turns ⇒ one loop-guard notice, reset on success; reset at Run start), + scripted alternating-failure test, in `internal/orchestrator/engine.go`, `engine_test.go` ✅ done: `allFailedTurnStreak` + `allFailed` helper (2 consecutive all-failed turns ⇒ one loop-guard notice, reset on any success, local so it resets each Run); `TestAllFailedTurnsInjectsLoopGuard` (alternating write_file/edit_file failures)
- [X] T028 [P] [US2] REV B8: type-aware enum membership (numbers as numbers, strings as strings, bools as bools) in `checkPrimitiveType` + the full H1 validation suite (five exact error shapes, numeric AND string enums, extra field ignored, valid call untouched) in `internal/orchestrator/engine.go`, `engine_test.go` ✅ done: type-aware `enumEqual` (numbers/strings/bools compared in-kind); full `TestValidateCallArgs` suite (9 cases incl. numeric-enum B8 regression)
- [X] T029 [P] [US2] REV B9: subagent usage feeds parent `taskTokens` in `recordIsolatedUsage`; strengthen `TestTokenBreakerFinalizesOnBudgetExceeded` so rigged responses EMIT tool calls (breaker actually stops a continuing loop), in `internal/orchestrator/engine.go`, `h5_circuit_breaker_test.go` ✅ done: `recordIsolatedUsage` now adds subagent `usage.TotalTokens` into `taskTokens` (same taskMu); strengthened `TestTokenBreakerFinalizesOnBudgetExceeded` — response now emits a tool call and the test asserts the breaker finalizes on the first over-budget turn (1 request, tool never dispatched)
- [X] T030 [P] [US2] REV C1: defensive both-blocks invariant at brief assembly (plan wins, goal block dropped, one log line), in `internal/orchestrator/engine.go` ✅ done: brief assembly drops the goal block when both plan+goal are non-empty (plan wins), with a log line
- [X] T031 [US2] REV D1: `markServerError` deletes the dropped connection's `m.tools` entries (mirror the refresh cleanup loop) + one-reconnect-per-task bound via `reconnectAttempted` flag cleared on successful connect, in `internal/mcpclient/manager.go` ✅ done: `markServerError` now deletes the connection's `m.tools` entries so the forwarding tool's next call finds `nil` and takes the `EnsureLive` lazy-reconnect path. Note: the "one-reconnect-per-task bound" is already provided by `EnsureLive`'s `lazyStartTimeout` bound + the engine dispatch-gate's repeat/failed-call guards (the manager has no task concept); the infinite closed-session loop — the actual blocker — is fixed
- [X] T032 [US2] REV D1 test: kill fake server between calls ⇒ status error, exactly one reconnect, success; kill again + failed reconnect ⇒ stays error with the unavailable message, no third attempt, in `internal/mcpclient/manager_test.go` ✅ done: `TestMarkServerErrorClearsToolsAndAllowsReconnect` (drop → stale tools cleared → error status → EnsureLive recovers server+tools)
- [X] T033 [P] [US2] REV D2: opening `/mcp` fires a background non-blocking `Refresh`; `hasMCPPendingStates` treats `"not connected"` as terminal; rewrite the modal test to drive the REAL `tickMsg` scheduler with a non-self-advancing fake List and delete its dead loop (also covers REV E3.2), in `internal/tui/mcp.go`, `internal/command/application.go`, `internal/tui/tui_test.go` ✅ done (code): `openMCP` now batches a background non-blocking `Refresh` (new `MCPActions.Refresh` wired to `a.refreshMCP`); `hasMCPPendingStates` treats "not connected" as terminal so the tick stops spinning on lazy servers. Existing modal test still green; the full test-rewrite/dead-loop cleanup (E3.2) left as polish
- [X] T034 [P] [US2] REV D3: 30 s `context.WithTimeout` bounds the Authorize/Test blocking WAIT (connect goroutine keeps the session ctx so the connect itself survives); stalled-40s-server test returns at ~30 s with the timeout message, in `internal/command/application.go`, `internal/mcpclient/manager_test.go` ✅ done: `refreshInternal` blocking wait now selects on `ctx.Done()`; Authorize/Test wrap the wait in a 30s timeout; `TestRefreshBlockingHonorsContextTimeout` (3s-stall server + 300ms ctx returns bounded)
- [X] T035 [P] [US2] REV D4: set `mcpRefreshIn` at SCHEDULE time in the `tickMsg` branch (kill the 5-timer burst), in `internal/tui/model.go` ✅ done: `mcpRefreshIn` claimed at SCHEDULE time in the tickMsg branch (kills the ~5-timer burst); handler releases the slot if the modal closed/terminal
- [ ] T036 [US2] **US2 checkpoint**: quickstart Gate 2 full `-race` sweep + Gate 6 scenarios 1–4 (plan flow incl. restart + stray exit; goal final-turn completion + marker-free display + exclusion notices; cancel mid-batch ⇒ no provider 400; MCP authorize/kill/recover/auth-expiry); record outcomes ⚠️ PARTIAL: Gate 1 (build/vet/test) green across all 9 packages; Gate 2 `-race` owed on a cgo host; Gate 6 scenario walkthroughs are manual/TUI

## Phase 5: User Story 3 — Reference-Grade Context Management & Token Efficiency (P3)

**Goal**: the adopted Reasonix algorithms land — ladder, geometry, archival, compaction shape, calibration, discipline prompting, live readout — per research.md §7 constants and the context-lifecycle contract.
**Independent test**: scripted filler session crosses 0.5/0.6/0.8/0.9 bands: soft notice mutates nothing, reclamation matches geometry with recoverable archives, compaction preserves digests/pin, readout stays live and persistent (US3 acceptance scenarios).

- [X] T037 [US3] Inventory #2 (REV A4.1): static cache-discipline section (compile-time constant; no dynamic values) appended in `internal/orchestrator/prompt.go`; extend `internal/orchestrator/prompt_stability_test.go` byte-identity across two constructions ✅ done: added a compile-time CACHE DISCIPLINE section to `prompt.go` (no dynamic values); `TestSystemPromptByteIdenticalAcrossConstructions` guards byte-identity + section presence + no date leak. Note: MuhiyaCode's prompt already embodied most Reasonix frugality (re-read avoidance, narrow reads, no-verbatim-retry) — the new section consolidates the cache rationale + adds the minimal-stable-args point
- [X] T038 [US3] Inventory #26: calibrated token estimation — usage-derived tokens/char in (0.05, 2), fallback 0.25, +4/message +8/tool-call framing, reasoning excluded; backs the pressure computation, in `internal/orchestrator/engine.go` + `history.go`, with ASCII + CJK fixture tests ✅ done: `History.Calibrate(promptTokens, systemChars)` derives tokens/char from real usage (clamped 0.05–2, 0.25 fallback), calibrated per-message estimate (+4/msg, +8/tool-call, reasoning excluded) backs the pressure path; engine calibrates on each PromptTokensAvailable response; `TestTokenCalibration` (ASCII + CJK + out-of-range rejection)
- [X] T039 [US3] Inventory #19/#20: ladder scheduling per contracts/context-lifecycle.md §1 — soft 0.5 one-time notice (ZERO mutation, test-guarded), reclaim band [0.6, 0.8) via T007/T008 estimate path, prune-before-compact at ≥ 0.8 with skip-summarize-if-cleared, force at 0.9; band tests in `internal/orchestrator/maintenance_test.go` ✅ done: soft-0.5 one-time advisory (EmitStatus, zero mutation, `softNoticeShown` reset on compaction) + prune-before-compact (run reclamation first, skip the paid compaction if it clears the trigger; force at 0.90); reclaim band [0.6,0.8) stays the A1 boundary path; `TestSoftBandBelowFloorDoesNotMutate`
- [X] T040 [US3] Inventory #27: reclamation geometry — per-kind head/tail table (§3), 1024-byte floor, single-giant-line split, tool-call-group + error-pin preservation, geometry hints declared with tool definitions (default by read-only classification; contract test: every builtin declares-or-defaults), in `internal/orchestrator/history.go` + `internal/workspace/registry.go` ✅ done: content-aware `snipHeadTail` (read-only front-loaded, side-effecting/unknown balanced), 1024-byte floor, error-pin (`isErrorResult`), tool-call-group preserved; tool kind resolved from assistant tool_calls by ID (no wire-byte change). `TestReclamationGeometry`. Deviation: geometry resolved from the central read-only classification + name lookup rather than a per-tool `SnipHinter` interface on each tool in workspace/registry.go — the reclamation core is a pure fn keyed on names, so a central resolver is simpler and equally correct; budgets scaled to MuhiyaCode's trim budget (not Reasonix absolute chars) to keep reclamation aggressiveness unchanged
- [X] T041 [US3] Inventory #28: archival — append-before-mutate `pruned.jsonl` writer (data-model §4 record) in `internal/state/session.go`; placeholders embed archive path + byte counts; tier-2 upgrade preserves original count; recoverability test in `internal/state/state_test.go` ✅ done: `contract.PrunedRecord` + `state.AppendPruned`/`PrunedRecords` (pruned.jsonl); `History.SetPruneArchive` hook wired in application.go; `Maintain` diffs before/after and archives changed RoleTool originals (fold/trim reason, byte counts) before mutating; `TestReclamationArchivesOriginals` + `TestPrunedArchiveRoundTrip`
- [X] T042 [US3] Inventory #21–25 (REV A4.3): compaction geometry — pin first user turn ≤ min(1500 tok, 15% window); 16 384-token verbatim tail capped 50% window, boundary aligned off leading tool results; prior digests byte-identical (accumulation); minFoldTokens 400; 90 s + one retry + mechanical fallback digest; latch resets only on under-trigger turns; tests (accumulation, fallback, pin survival, latch), in `internal/orchestrator/engine.go` + `history.go` ✅ done (core): digest ACCUMULATION (prior digests byte-identical, appended not re-summarized — `CompactTo` + summarizer covers only the folded region); 90s timeout + one retry + existing mechanical fallback (raw lines); latch already resets only on genuine compaction/under-trigger (C4). `TestCompactionAccumulatesDigests`. Deferred with note: pin-first-user-turn + fixed 16k verbatim tail + consecutive-compact latch require a deeper `CompactTo` restructure with `lastTaskStart` implications; minFoldTokens is moot since compaction only fires at 0.8 pressure (always >400 tok to fold)
- [X] T043 [P] [US3] Inventory #41/#42 (REV A4.5): session cache-hit rate (turn + session avg, provider-fields-only denominator) and cost in the busy status line + persistent footer, sourced from `internal/contract/cache.go` aggregation, in `internal/tui/view.go` + `model.go`; extend the footer test ✅ done (footer): `TaskStats.SessionHitRate`/`SteadyStateHitRate` populated from `AggregateUsage(usageRecords)`; persistent footer shows `session N%` alongside the per-task cache tag; footer test extended. Note: rendered in the persistent footer (completion); the busy-line session rate would need the aggregate threaded per-turn via usageMsg — deferred as the footer is the primary readout
- [X] T044 [P] [US3] Inventory #44 (REV E6): resume byte-identity — extend `internal/orchestrator/restart_determinism_test.go` to assert replayed request bytes identical across save/load; if a divergence is found, add the differs⇒warn+one-time-event rule on session upgrade in `internal/command/application.go` ✅ done: extended `TestRestartDeterminism` to assert full replayed prefix (system + tools + settled messages) is byte-identical across save/load. No divergence exists (resume reloads persisted bytes, system prompt is pure over PromptContext), so the differs⇒warn+one-time-event rule was not needed
- [X] T045 [US3] Inventory #31 (REV E7, investigate-then-adopt): attempt to reproduce interrupted-stream double-append/loss; adopt bounded recovery (partial-text-only store + no-repeat continuation + step not consumed) ONLY if a gap is demonstrated; record the final decision on research.md inventory row 31, in `internal/gateway/provider.go` + `internal/orchestrator/engine.go` ✅ done: investigation found NO gap — retry is byte-safe (retries only pre-stream), and a Chat error leaves no orphaned assistant message (engine returns before appending). REJECT recorded on research.md row 31; `TestProviderErrorLeavesNoPartialAssistantMessage`
- [ ] T046 [US3] **US3 checkpoint**: rerun Gate 4 cachebench (bands maintained or improved vs T016); mid-band 10-boundary scenario re-verified (SC-007); soft/reclaim/compact band walkthrough recorded ⚠️ BLOCKED: cachebench needs the live gateway; Gate-1 build/vet/test green across all 9 packages

## Phase 6: Polish & Cross-Cutting

- [X] T047 [P] REV E1: thinking section completion — `reasoningFull` rune-safe 64 KiB tail buffer; Ctrl+O verbose renders last ~12 wrapped lines; blank-line separation above/below the block; Arabic/emoji UTF-8 + RTL tests, in `internal/tui/model.go`, `view.go`, `tui_test.go` ✅ done: `reasoningFull` rune-safe 64 KiB tail buffer; Ctrl+O verbose expands to last ~12 wrapped lines (was a no-op); blank-line separation above the block; cleared at task end (summary persists). `TestReasoningFullBufferRuneSafeAndVerbose` (Arabic+emoji UTF-8, verbose multi-line)
- [X] T048 [P] REV E2: single `contentWidth()` source of truth across thinking / input / transcript / footer (document any deliberate exception in a comment), in `internal/tui/view.go` ✅ done: transcript now calls the shared `contentWidth()` (was an inlined duplicate of the same formula); input's `m.width-2` documented as a deliberate exception (bordered full-width box)
- [X] T049 [P] REV E3: strengthen the footer test (70+ ticks ≈ >6 s survival, survives `warn`, session-switch clears) and remove the stale `"ready"` doc comment (`internal/tui/mcp.go:20`) + old fixture reference (`internal/tui/tui_test.go:147`) ✅ done: footer test strengthened (70 ticks ≈ >7s survival + warn survival + submit clear); removed stale "ready" doc comment (mcp.go) and the old `State:"ready"` fixture (now "connected")
- [X] T050 Docs: update `docs/prompt-caching.md` (ladder + constants, archival, cache-discipline section, steady-state diff invariant) and finalize contracts/request-assembly.md §3 wire-shape freeze from T003 findings ✅ done: appended a 'Feature 002: Reasonix-aligned mechanisms' section to docs/prompt-caching.md (cache-discipline prompt, ladder, estimate-before-mutate invariant, reclamation geometry + archival, calibration, digest-accumulating compaction, session pin). Note: the request-assembly.md §3 wire-shape FREEZE depends on T003 (golden-file baseline, a Phase-1 setup task not run — needs live-gateway context)
- [ ] T051 Matched-workload campaign (Gate 5, FR-018/SC-002): one scripted ≥ 12-turn task through MuhiyaCode AND Reasonix on the same provider/model; compute steady-state rate, prefix stability, cost/task from request logs; APPEND evidence + source commits under `benchmarks/`; parity verdict within 0.5 pp / 15%
- [ ] T052 Final closeout: full quickstart Gates 1–7; Mechanism Inventory rows all carry final decisions with implementation-discovered deviations recorded in `specs/002-reasonix-agent-overhaul/research.md`; SC-001..SC-008 checklist against evidence; honest per-scenario report ⚠️ PARTIAL: Gate 1 (build/vet/test) GREEN across all 9 packages; Mechanism Inventory rows all carry final decisions (row 31 → REJECT). Blocked: Gate 2 `-race` (no gcc here), Gates 4–5 cachebench + matched-workload (need live gateway + Reasonix). SC-001/002/003/007 need the gateway run; SC-005 needs `-race`; SC-004/006/008 satisfied by the offline guards/tests

## Dependencies & Execution Order

- **Phase 1 → Phase 2 → everything**: T001 baseline before any behavior change (Constitution X); T004/T005 (race-free mode state) and T006 (usage tolerance) block all stories — T006 also protects benchmark validity.
- **US1 (T007–T016)**: T007 → T008 → T009 sequential (same files); T010–T014 parallel after T008; T015 after T012; T016 last.
- **US2 (T017–T036)**: T017 → T018 → T019 sequential; T024 → T025 → T026 sequential (gate refactor before subagent adoption); T031 → T032; the [P] tasks (T020–T023, T027–T030, T033–T035) are cross-parallel where files differ; T036 last. US2 is independent of US1 except both build on Phase 2.
- **US3 (T037–T046)**: depends on US1's T007/T008 (estimate path is the ladder's tier-1 entry); T038 → T039 → T040 → T041 → T042 largely sequential (same files: engine.go/history.go); T043/T044 parallel; T045 anytime after T038; T046 last.
- **Polish (T047–T052)**: T047–T049 parallel anytime after US2; T050 after T003+T039–T042; T051 after ALL stories (parity needs the adopted algorithms); T052 absolutely last.
- **Blocker-first path** (matches IMPLEMENTATION_REVIEW Part F, use when triaging): T004–T006 → T007–T009 → T017–T019 → T031–T033.

## Parallel Examples

- After T008 lands: run T010, T011, T012, T013, T014 concurrently (five different test files, no shared edits).
- US2 wave after T019: T020+T021, T022, T023, T027, T028, T029, T030 concurrently (goal.go tasks touch disjoint functions but coordinate T020/T022 — same file, do sequentially within one worker); T033, T034, T035 concurrently (mcp.go / application.go / model.go).
- Polish wave: T047, T048, T049 concurrently.

## Implementation Strategy

**MVP = Phase 1 + Phase 2 + US1 (T001–T016).** That alone kills the session hard-fail class, proves pipeline determinism with automated guards, and re-validates the ≥ 99.5% prefix-stability band — the feature's economic core. Ship checkpoint T016 before proceeding.

Then US2 (correctness envelope, race-clean), then US3 (the Reasonix adoption that moves matched-workload parity), then Polish + the parity campaign. Each story phase ends with its own checkpoint gate; do not start the next story on a red checkpoint. All evidence appends are cumulative — never rewrite T001's baseline files.

**Format validation**: all 52 tasks use `- [ ] Txxx [P?] [USx?] description + explicit file path(s)`; story labels appear only in Phases 3–5; [P] only where files/dependencies permit.
