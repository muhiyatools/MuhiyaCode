---
name: external-only
description: An out-of-workspace skill (via MUHIYA_SKILLS_DIR). It must appear in the /skills modal but NOT in the system-prompt listing (workspace-only advertisement).
---

# External Only

Used by quickstart Scenario 6 step 5 to verify external-root skills are reachable
via the manual /skills flow but never advertised in the stable prefix.
