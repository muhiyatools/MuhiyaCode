This skill file intentionally has no YAML frontmatter, to exercise the
no-description rendering path (contract skills-delivery.md §2 / §6). The
discovered name falls back to the directory basename "bare-skill".
